# AGENTS.md — Vibe-Research 项目纪律

> 2026-08-11 缠论信号翻车（全量点位重标 = 选择层未来函数）后固化的强制性规则。
> 详细审计过程见 `docs/审计执行进展.md` 第 13 节。

## 0. 底线：无未来函数必须可被机械证明

任何回测/信号必须满足三层无未来函数，且每层都有测试或代码结构保证：

1. **计算层**：指标只使用截至 T 收盘的数据；
2. **执行层**：信号确认后才成交（T+1/T+2 开盘，盘中排除未收盘末根）；
3. **选择层**：信号身份不得随数据增长重标——缠论必须用 `analyze_chan_locked`，
   SMC 扫荡必须逐 bar 因果化；回测口径必须通过截断一致性测试
   （`tests/test_no_lookahead.py`：截断结果 ⊆ 全量结果）。

“设计时注意”不算保障，只有测试才算。任何新信号在合入前必须补对应的
截断一致性测试，并保证全量 `pytest` 通过。

## 1. 信号准入五道关

任何新信号/策略要标记为“通过”，必须依次完成并留档：

1. **假设卡**：可证伪，写清信号定义、数据时间、成交规则、失败标准；
2. **单元测试**：信号实现有单测；
3. **截断一致性测试**：按 `test_no_lookahead.py` 模式验证无未来函数；
4. **基准对照**：随机日期基准 + 同板块异股对照，方向/净收益/组合三级；
5. **样本外验证**：参数固定后一次性样本外（新股票池 / 更早时段）。

任何一道不通过，结论只能标“未验证”或“失败”，禁止写“已验证 / 有优势”。

## 2. 两轮正反辩驳（强制）

所有策略制定（含调参、信号组合、卖出规则、做T、因子选择）必须经过
**两轮正反辩驳**并留档：

- **第一轮**：正方提出假设 + 证据 → 反方唱反调（选择偏差 / 未来函数 /
  过拟合 / 样本 / 容量）→ 正方答辩（每条标承认/反驳，给仓库内证据）；
- **第二轮**：反方复辩 → 正方终辩，输出明确的行动项。

每轮产物存入 `docs/`（参考 `docs/辩驳-组合卖出信号.md` 与 `-答辩.md` 的格式），
实验记录写入 `backend/quant_framework/data/experiments.csv`（含失败实验）。
**没有完成两轮辩驳并留档的策略，不得进入下一步验证，更不得标记“通过”。**

## 3. 实验记录与结论口径

- 所有实验（含失败）写入 `experiments.csv`，必须含样本数、费用/涨跌停约束、
  code_version、失败原因；
- 结论一律以修正后口径为准（锁定缠论 / 因果 SMC / 逐 bar 前缀），
  旧全量口径的“已验证”结论全部视为已撤回（见 docs 横幅）；
- 数字必须可复现：seed、数据源、窗口、参数全部写进报告；
- 禁止“看过结果后挑选信号/参数再宣称验证”（选择偏差），
  参数敏感性不足 3 个点、样本池只有一年，只能算“候选”，不算“验证”。

## 4. 代码习惯

- 新信号默认 `analyze_chan_locked` / 逐 bar 前缀计算；
- K 线只展示成果性标定（B/S、顶/底、积/派/扫/突/破/变），复杂参数放 tooltip；
- 展示性 API 若因性能继续用全量口径，必须在该 API 文档标注
  “展示点位可能随窗口漂移，不构成交易信号”；
- 研究脚本写入 `backend/quant_framework/run_*.py`，测试写入 `tests/`。
