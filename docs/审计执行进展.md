# 对抗审计执行进展（答辩 §12 落地）

> 更新时间：2026-08-10　对应对抗审计报告与答辩报告

## 1. 实验日志补录 ✅

`backend/quant_framework/data/experiments.csv` 已补录 7 条调参/审计历史（含失败）：
ATR 顶底过滤、三卖预警、信号频率限制、分时量修复、新增策略对比、分时信号评估、
对抗审计本身；现有 13 条，均已标注 passed=False（未完成盲测/收益验证）与失败原因。

## 2. 做T引擎涨跌停/停牌成交约束 ✅

`t_backtest.py::run_t_backtest` 新增 `enforce_limit=True`（默认开启）、
`limit_up_pct/limit_down_pct=0.098`：
- 信号执行：涨停开盘 B 点买不进、跌停开盘 S 点卖不出 → 跳过并计数；
- 收盘强制回补：涨停收盘买不回 / 跌停收盘卖不出 → `blocked_restore`；
- summary 新增 `blocked_trades`。

新增 3 个单测（`test_t_backtest.py`）：跌停开盘挡 S、涨停开盘挡 B、涨停收盘挡回补。
全量测试 34 passed。

注：`run_band_t_backtest` 尚未接入同一约束（下一步）。

## 3. 分时信号评估改 T+1 开盘成交口径 ✅

`run_minute_signal_eval.py` 基准从“信号 bar 收盘”改为“信号后下一根开盘成交，
持有 3/6 根收盘”，当日最后信号跳过（做T不隔夜）。重跑结果：

| 策略 | 方向 | 样本 | 30分钟准确率 |
|---|---|---|---|
| SMC 扫 | 买 | 87 | 61%（原 67%） |
| SMC 卖 | 卖 | 92 | 54% |
| 缠论 | 卖 | 13 | 46% |

SMC 买点仍有弱优势，但比原口径低 6 个百分点——原报告偏乐观。

## 4. 做T收益集中度拆分 ✅

`run_pnl_breakdown.py`（30分K、最近 100 交易日、半仓、含费用、含涨跌停约束）：

| 股票 | T 净收益 |
|---|---|
| 300223 | **+27199** |
| 000636 | -3574 |
| 600487 | -1200 |
| 000063 | -5 |
| 516080 | -48 |

总收益几乎全部来自 300223 单票——收益集中度坐实（前 1 只占比 >100%）。

## 5. 全市场随机 50 只盲测 ✅（初步）

`run_blind_test.py`：东财全市场随机 50 只（排除科创板/北交所/ST，seed 固定），
腾讯日K约 800 根（≈3.3 年，接口上限，未满 5 年），信号后下一日开盘成交。

| 策略 | 方向 | 样本 | 5日准确率 | 5日平均收益 |
|---|---|---|---|---|
| 缠论 | 买 | 606 | **88%** | +5.63% |
| 缠论 | 卖 | 1474 | **63%** | -1.14% |
| ATR | 买/卖 | 356/421 | 48% | +0.62%/+1.42% |
| SMC | 买 | 148 | 51% | +0.61% |
| SMC | 卖 | 233 | **40%（反向）** | +2.35% |

重要发现：
- 缠论 B/S 在随机样本上有方向优势（未计费用、未对市场 beta/均值回归去偏，需继续审计）；
- **ATR/SMC 在自选池上的“优势”在盲测中消失，SMC 日K卖点甚至反向**——
  印证此前结论依赖样本；
- 缠论买点 88% 高得可疑，最可能是“买点出现在下跌后、5 日均值回归”而非独立 Alpha，
  需要对比随机日期基准收益。

## 6. 尚未完成（按优先级）

1. 缠论买点 88% 的基准去偏：与“随机日期后 5 日收益”对比，并按年/月/市场状态拆分；
2. 盲测加交易成本与涨跌停约束后的净收益回测（当前仅方向准确率）；
3. `run_band_t_backtest` 接入涨跌停约束；
4. 分钟级跨行业盲测：腾讯分钟K仅 320~800 根，需换数据源（mootdx/东财）或接受缩短窗口；
5. 分年/分月 × regime 交叉拆分的正式报告（脚本逻辑已存在于 regime 报告，尚未合入本文档数据）。
